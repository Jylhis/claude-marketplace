{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Review this bash command. If it contains destructive operations (rm -rf /, drop database, etc.), respond with BLOCK. Otherwise respond with ALLOW."
          }
        ]
      }
    ]
  }
}
